import { StatusBar } from 'expo-status-bar';
import React from 'react';
import { StyleSheet, Text, View } from 'react-native';

const ProfilePage = () =>{
    return(
        <View style={{flex:1}}>
            <Text style={{alignSelf:'center'}}>Profilepage</Text> 
        </View>
    )
}
export default ProfilePage;